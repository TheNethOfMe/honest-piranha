import React, { Fragment } from "react";

const NotFound = () => {
  return (
    <Fragment>
      <h1 className="notfound-page">Sorry, that page doesn't exist!</h1>
    </Fragment>
  );
};

export default NotFound;
